import { FacilityAuditReasonPipe } from './facility-audit-reason.pipe';

describe('FacilityAuditReasonPipe', () => {
  it('create an instance', () => {
    const pipe = new FacilityAuditReasonPipe();
    expect(pipe).toBeTruthy();
  });
});
